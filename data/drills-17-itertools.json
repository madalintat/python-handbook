[{"n": 1, "q": "`groupby` groups", "options": ["All equal values, wherever they appear", "Consecutive runs of equal keys", "The input after sorting it", "Values by their hash"], "answer": 1, "why": "Which is why the input must already be ordered by the same key, and why a mismatched key produces a result that looks almost right."}, {"n": 2, "q": "The group `groupby` yields is", "options": ["A list", "An iterator sharing the source, consumed when you advance to the next group", "A tuple", "A copy"], "answer": 1, "why": "So `list(groupby(...))` gives you keys and a set of exhausted iterators. Convert each group as you go."}, {"n": 3, "q": "When the input cannot be sorted, grouping is better done with", "options": ["`groupby` anyway", "A `defaultdict(list)`, in one pass with no ordering requirement", "`sorted` with a key", "`Counter`"], "answer": 1, "why": "And the groups come back as real lists you can keep."}, {"n": 4, "q": "`takewhile` differs from `filter` in that", "options": ["It stops at the first failure instead of checking every element", "It is eager", "It returns a list", "It takes a key rather than a predicate"], "answer": 0, "why": "Which is what lets it work on an unbounded source. `dropwhile` skips that same leading run."}, {"n": 5, "q": "`islice(source, 3)` versus `list(source)[:3]`", "options": ["Identical", "`islice` reads three values; the slice reads all of them first", "`islice` returns a list", "The slice is lazy"], "answer": 1, "why": "And on an unbounded source the slice never returns at all."}, {"n": 6, "q": "`chain.from_iterable(batches)` is the lazy version of", "options": ["`[item for batch in batches for item in batch]`", "`zip(*batches)`", "`sorted(batches)`", "`batches[0] + batches[1]`"], "answer": 0, "why": "It holds one item at a time and reads each batch only when it reaches it."}, {"n": 7, "q": "`pairwise([1, 4, 9])` yields", "options": ["`(1, 4)` then `(4, 9)`", "`(1, 4)` then `(9, None)`", "`(1, 9)`", "`[1, 4, 9]`"], "answer": 0, "why": "Overlapping neighbours, from any iterable, in one pass. The old `zip(x, x[1:])` needs a sequence."}, {"n": 8, "q": "`tee(it, 2)` costs", "options": ["Nothing", "Memory proportional to how far apart the two consumers are", "A full copy immediately", "One extra pass over the source"], "answer": 1, "why": "So `tee` with one fast and one slow consumer is a memory leak with a friendly name."}, {"n": 9, "q": "`map(lambda x: x * 2, xs)` compared with the comprehension is", "options": ["Faster and shorter", "Longer and slower; `map` earns its place with a named function", "Identical in every way", "Lazy where the comprehension is not"], "answer": 1, "why": "`map(int, fields)` is the good case. Both return iterators either way."}, {"n": 10, "q": "`filter(None, values)` does what?", "options": ["Raises, because `None` is not callable", "Drops the falsy values", "Keeps only the `None`s", "Returns everything"], "answer": 1, "why": "The one `filter` idiom that is genuinely shorter than the comprehension."}, {"n": 11, "q": "`@cache` requires its arguments to be", "options": ["Hashable, because they become a dict key", "Immutable strings", "Positional", "Annotated"], "answer": 0, "why": "A tuple and a list of the same values are different keys, so the conversion has to be consistent."}, {"n": 12, "q": "`@cache` on a method", "options": ["Caches per instance", "Keeps every `self` ever passed alive for the life of the process", "Raises", "Is cleared when the instance is collected"], "answer": 1, "why": "A memory leak that looks like a speedup. Cache a plain function that takes the values it needs."}, {"n": 13, "q": "`@cache` versus `@lru_cache(maxsize=n)`", "options": ["`cache` is unbounded; `lru_cache` keeps the n most recent", "`cache` is faster but not thread safe", "They are identical", "`lru_cache` is deprecated"], "answer": 0, "why": "Bounded is the safer default for anything driven by external input."}, {"n": 14, "q": "Most uses of `functools.reduce` are", "options": ["The only way to express the operation", "`sum`, `min`, `max`, `any`, `all` or `math.prod` in disguise", "Faster than the equivalent loop", "Required for immutability"], "answer": 1, "why": "Which is why it moved out of builtins in Python 3. Keep it for genuinely unusual operations."}, {"n": 15, "q": "`functools.singledispatch` replaces", "options": ["A `match` statement", "A tower of `isinstance` checks, and lets other modules register cases", "A class hierarchy", "A dict of handlers"], "answer": 1, "why": "Dispatch is on the type of the first argument, and it is extensible without editing the original."}]