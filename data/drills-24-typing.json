[{"n": 1, "q": "At run time, an annotation", "options": ["Is metadata stored on the object and checked by nothing", "Raises `TypeError` when the argument does not match", "Converts the argument", "Is removed by the compiler"], "answer": 0, "why": "`def double(n: int)` will happily take a string and return `\"haha\"`."}, {"n": 2, "q": "`Optional[int]` means", "options": ["`int` or `None`", "The argument may be omitted", "The argument has a default", "Any integer-like value"], "answer": 0, "why": "A parameter is optional because it has a default. `Optional` is about the value, not the call."}, {"n": 3, "q": "The modern spelling of `Optional[str]` is", "options": ["`str | None`", "`str?`", "`Maybe[str]`", "`typing.Optional[str]` still"], "answer": 0, "why": "And `int | str` for `Union[int, str]`. Prefer the bar."}, {"n": 4, "q": "`list[str]` rather than `typing.List[str]` has worked since", "options": ["3.9", "3.12", "It has not; `List` is required", "3.5"], "answer": 0, "why": "The capitalised forms still exist and you will read them, but there is no reason to write them."}, {"n": 5, "q": "For a parameter you only iterate, annotate", "options": ["`Iterable[T]`", "`list[T]`", "`Sequence[T]`", "`Any`"], "answer": 0, "why": "A caller with a tuple is not doing anything wrong, and the narrow annotation refuses code that would have worked."}, {"n": 6, "q": "`list[Dog]` where `list[Animal]` is expected is", "options": ["Refused, because the function could append a `Cat` to the caller's list", "Accepted, because `Dog` is an `Animal`", "Accepted with a warning", "Refused because lists are invariant in both directions only for builtins"], "answer": 0, "why": "`Sequence[Animal]` is fine, because a sequence cannot be appended to."}, {"n": 7, "q": "Return types should be", "options": ["Concrete, so the caller knows what they can do with the value", "As general as the parameters", "`Any` when unsure", "Omitted when obvious"], "answer": 0, "why": "Be generous in what you accept and specific about what you hand back."}, {"n": 8, "q": "`Literal[\"left\", \"right\"]` is enforced", "options": ["By the checker only; any string passes at run time", "By the interpreter, which raises on anything else", "By `__post_init__`", "Only inside a dataclass"], "answer": 0, "why": "Which is exactly why a misspelled literal is worth catching statically."}, {"n": 9, "q": "A `TypedDict` at run time is", "options": ["An ordinary dict; nothing is created and nothing is checked", "A new class with slots", "A validating wrapper", "A `NamedTuple`"], "answer": 0, "why": "It buys a checker that knows `row[\"nmae\"]` is wrong. When you control the shape, a dataclass is better."}, {"n": 10, "q": "A class satisfies a `Protocol` by", "options": ["Having the right methods, with no inheritance and no registration", "Inheriting from it", "Calling `register`", "Declaring it in `__protocols__`"], "answer": 0, "why": "Duck typing with a name a checker can verify, including for classes you do not own."}, {"n": 11, "q": "`Any`", "options": ["Switches checking off for every expression it touches, in both directions", "Means \"some type I have not decided yet\"", "Is the same as `object`", "Accepts anything but permits nothing"], "answer": 0, "why": "`object` is the honest annotation for \"genuinely anything\": it accepts everything and permits nothing until you narrow."}, {"n": 12, "q": "After `if user is None: return ...`, a checker knows the rest of the function has", "options": ["A non-`None` user", "A `User | None` still", "An `Any`", "Nothing; narrowing needs `assert`"], "answer": 0, "why": "`isinstance`, `assert`, truthiness tests and early returns all narrow."}, {"n": 13, "q": "`def first[T](items: Sequence[T]) -> T` says", "options": ["The return type is whatever the elements are", "The return type is `Any`", "`T` must be declared with `TypeVar` first", "`items` must be a list"], "answer": 0, "why": "The 3.12 syntax scopes `T` to the function, which is what everybody assumed the old `TypeVar` did."}, {"n": 14, "q": "`Callable[[int], int]` describes a function that", "options": ["Takes one `int` and returns an `int`", "Takes any arguments and returns an `int`", "Takes a list of ints", "Returns a callable"], "answer": 0, "why": "`Callable[..., str]` is the escape hatch when the parameters genuinely vary."}, {"n": 15, "q": "`from __future__ import annotations`", "options": ["Makes every annotation a string, so a class can refer to itself without quotes", "Enables run-time checking", "Is required for `X | None`", "Speeds up mypy"], "answer": 0, "why": "Anything reading annotations at run time then has to resolve them, which `typing.get_type_hints` does."}]