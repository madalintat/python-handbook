[{"n": 1, "q": "An iterable is something that", "options": ["Can give you an iterator, via `__iter__`", "Produces values with `__next__`", "Has a length", "Can be indexed"], "answer": 0, "why": "An iterator is the thing with `__next__`. It also has `__iter__`, which returns itself."}, {"n": 2, "q": "Is a list an iterator?", "options": ["Yes", "No; it is an iterable, and each walk asks for a fresh iterator", "Only when it is non-empty", "Only inside a `for` loop"], "answer": 1, "why": "Which is why a list can be walked as many times as you like."}, {"n": 3, "q": "`for item in things` begins by calling", "options": ["`iter(things)`", "`things[0]`", "`len(things)`", "`next(things)`"], "answer": 0, "why": "Then `next` repeatedly until `StopIteration`, which the loop catches."}, {"n": 4, "q": "`list(it)` twice on the same iterator gives", "options": ["The same values both times", "The values, then an empty list", "A RuntimeError on the second call", "The values twice over"], "answer": 1, "why": "Exhaustion is reported as emptiness rather than as an error, which is why the bug travels rather than raising."}, {"n": 5, "q": "Which of these returns an iterator?", "options": ["`range(10)`", "`d.items()`", "`zip(a, b)`", "`sorted(x)`"], "answer": 2, "why": "`map`, `filter`, `enumerate`, `reversed` and an open file are iterators too. `range` is a lazy sequence and views are reusable."}, {"n": 6, "q": "`__iter__` returning `self` makes the object", "options": ["An iterator, so it has one pass in it", "A reusable iterable", "A sequence", "A generator"], "answer": 0, "why": "Returning a fresh iterator each time is what makes something walkable more than once."}, {"n": 7, "q": "`len()` on an iterator", "options": ["Returns the number of values left", "Raises TypeError", "Returns 0", "Consumes it and returns the count"], "answer": 1, "why": "An iterator does not know how many remain, and finding out would consume them."}, {"n": 8, "q": "`if some_iterator:` tells you", "options": ["Whether it has values left", "Nothing; it is always true", "Whether it is exhausted", "Whether its first value is truthy"], "answer": 1, "why": "It defines neither `__bool__` nor `__len__`, so unit 03's default applies."}, {"n": 9, "q": "`x in some_iterator`", "options": ["Is a cheap check", "Walks it until it matches, consuming everything it passed", "Raises TypeError", "Restarts it afterwards"], "answer": 1, "why": "Which makes a membership test followed by a loop see only the remainder."}, {"n": 10, "q": "`next(it)` on an exhausted iterator", "options": ["Returns `None`", "Raises StopIteration", "Returns an empty value", "Restarts the iterator"], "answer": 1, "why": "`next(it, default)` returns the default instead, which is right when running out is a normal case."}, {"n": 11, "q": "`iter(callable, sentinel)` does what?", "options": ["Calls the callable repeatedly, stopping when the result equals the sentinel", "Iterates the callable's arguments", "Applies the callable to each item", "Raises; `iter` takes one argument"], "answer": 0, "why": "It turns any \"call until it says stop\" API into something a `for` loop can walk."}, {"n": 12, "q": "A class with `__getitem__` taking 0, 1, 2... and no `__iter__`", "options": ["Cannot be iterated", "Is iterable through the old protocol, stopping at IndexError", "Raises TypeError", "Is iterated in reverse"], "answer": 1, "why": "Which is why `isinstance(x, Iterable)` can say False for something a `for` loop handles perfectly well."}, {"n": 13, "q": "`iter(x) is x` is true for", "options": ["An iterator", "Any iterable", "A list", "A range"], "answer": 0, "why": "Which makes it the fastest way to settle whether the thing you are holding has one pass in it."}, {"n": 14, "q": "A function that takes an iterable and walks it twice", "options": ["Works for anything iterable", "Works for a list and silently misbehaves for a generator", "Raises for a generator", "Is always wrong"], "answer": 1, "why": "Either walk once, or materialise at the top with `items = list(items)` and say so."}, {"n": 15, "q": "The main thing laziness buys is", "options": ["Speed", "Unbounded sources, bounded memory, and work that is never done", "Thread safety", "Shorter code"], "answer": 1, "why": "And one `sorted()` or list comprehension in the middle of a chain forces everything through it, which loses all of it."}]