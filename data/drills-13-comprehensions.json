[{"n": 1, "q": "`(f(x) for x in items)` builds", "options": ["A tuple", "A generator, which produces values one at a time and stores none", "A list", "A set"], "answer": 1, "why": "Round brackets make it lazy. The other three forms build the whole collection immediately."}, {"n": 2, "q": "Where does the filtering `if` go?", "options": ["At the front, before the expression", "At the end, after the `for`", "Either; they mean the same", "Inside the expression"], "answer": 1, "why": "A trailing `if` decides membership. A leading `if/else` is a conditional expression and produces a value for every item."}, {"n": 3, "q": "`[x if x > 0 else 0 for x in items]` returns how many elements?", "options": ["The same number that went in", "Only the positive ones", "None", "It raises"], "answer": 0, "why": "A conditional expression must produce a value, so it cannot skip an item. There is no filtering form at the front."}, {"n": 4, "q": "`[(r, c) for r in rows for c in cols]` nests", "options": ["`rows` outermost, in the order you would write the loops", "`cols` outermost", "In parallel, like `zip`", "It is a syntax error"], "answer": 0, "why": "The expression comes first and the clauses then read in normal order, which is worth checking against your intuition once."}, {"n": 5, "q": "`[[f(c) for c in row] for row in grid]` produces", "options": ["A flat list", "A list of lists", "A generator", "A dict"], "answer": 1, "why": "A comprehension inside another one nests. Two `for` clauses in a single comprehension flatten."}, {"n": 6, "q": "A comprehension's loop variable after it finishes", "options": ["Holds the last value", "Does not exist; the comprehension has its own scope", "Holds `None`", "Holds the whole list"], "answer": 1, "why": "Which is why a comprehension can never quietly clobber a name you were using."}, {"n": 7, "q": "Which part of a comprehension is evaluated in the enclosing scope?", "options": ["The first iterable", "The expression", "The filter", "All of it"], "answer": 0, "why": "Which is why a comprehension in a class body can iterate a class attribute and cannot refer to one inside its body."}, {"n": 8, "q": "`[log(x) for x in items]` is", "options": ["The idiomatic way to run a loop", "A list of `None` that nobody wants, hiding the intent", "Faster than a `for` statement", "A syntax error"], "answer": 1, "why": "A comprehension is for producing a value. When the loop is for its effect, a `for` statement says so."}, {"n": 9, "q": "`any(row.failed for row in rows)` versus `any([row.failed for row in rows])`", "options": ["Identical", "The generator stops at the first true value and builds nothing; the list walks everything", "The list version is faster", "The generator version is wrong"], "answer": 1, "why": "The same holds for `all`, `sum`, `min` and `max`."}, {"n": 10, "q": "A generator bound to a name and used twice", "options": ["Works, producing the same values both times", "Is empty the second time, because it is exhausted after one pass", "Raises on the second use", "Restarts automatically"], "answer": 1, "why": "No memory and one pass is the trade the round brackets buy. Build a list when you need it twice."}, {"n": 11, "q": "`{v: k for k, v in d.items()}` assumes", "options": ["That the values are unique, and silently keeps the last one when they are not", "That the keys are strings", "That the dict is sorted", "Nothing"], "answer": 0, "why": "When the relationship is one-to-many, group into lists instead."}, {"n": 12, "q": "`list.pop(0)` costs", "options": ["Constant time, like `pop()`", "Linear time, because every remaining element shifts down", "Logarithmic time", "It raises on a long list"], "answer": 1, "why": "`collections.deque` is constant at both ends, which makes it the right type for a queue."}, {"n": 13, "q": "`collections.Counter` is", "options": ["A dict that starts every key at zero and counts an iterable in one pass", "An ordered list of counts", "A generator", "A set with multiplicities that cannot be read"], "answer": 0, "why": "`most_common(n)` ranks them, and it supports arithmetic, so `Counter(a) - Counter(b)` is what is in one beyond the other."}, {"n": 14, "q": "`ChainMap` is for", "options": ["Merging dicts into one", "Presenting several mappings as one, searched in order, without copying", "Chaining iterables", "Linking a dict to its parent class"], "answer": 1, "why": "Which keeps which layer a value came from, where merging into one dict throws that away."}, {"n": 15, "q": "A comprehension is faster than the equivalent loop mainly because", "options": ["It runs in C", "It appends with a dedicated instruction instead of looking up and calling `.append` each time", "It is parallelised", "It skips the loop variable"], "answer": 1, "why": "The difference is modest. The reason to prefer one is that it states what is being built in its first three words."}]