[{"n": 1, "q": "`sorted(x)` and `x.sort()` differ in that", "options": ["`sorted` returns a new list; `sort` reorders in place and returns `None`", "`sorted` is stable and `sort` is not", "`sort` accepts any iterable", "They are the same"], "answer": 0, "why": "`sorted` takes any iterable and always gives a list. `sort` exists only on lists."}, {"n": 2, "q": "The `key` function is called", "options": ["Once per comparison", "Once per element", "Once in total", "Only when two elements are equal"], "answer": 1, "why": "Which is why an expensive key costs n rather than n log n, and why this replaced Python 2's comparison function."}, {"n": 3, "q": "`key=len(words)` instead of `key=len` gives", "options": ["The same result", "A TypeError, because an integer is not callable", "A sort by the list's length", "A SyntaxError"], "answer": 1, "why": "`key` wants a function, so its value should be a name, a lambda, or an `itemgetter`, never a call with arguments applied."}, {"n": 4, "q": "`sorted(rows, key=lambda r: (r.dept, r.name))` sorts by", "options": ["Department, then name within each department", "Name, then department", "The tuple's length", "Nothing; tuples are not comparable"], "answer": 0, "why": "Tuples compare elementwise, which makes a tuple key the whole of multi-level sorting."}, {"n": 5, "q": "To sort by one field ascending and another descending, for numbers", "options": ["Negate the descending one inside the tuple key", "Pass `reverse=True`", "Sort twice with `reverse=True` on the second", "It cannot be done in one sort"], "answer": 0, "why": "`reverse=True` reverses the whole ordering, not one level of it."}, {"n": 6, "q": "Python's sort being stable means", "options": ["Elements that compare equal keep the order they were already in", "The order is the same on every platform", "It never raises", "Equal elements are removed"], "answer": 0, "why": "Which is what makes sorting twice work, and it is documented behaviour rather than an accident."}, {"n": 7, "q": "To sort by two fields where one cannot be negated, sort twice", "options": ["Most significant field first", "Least significant field first", "In either order", "It cannot be done"], "answer": 1, "why": "The later sort's groups preserve the earlier sort's order, because the sort is stable."}, {"n": 8, "q": "`sorted(x, reverse=True)` versus `reversed(sorted(x))`", "options": ["Identical", "`reverse=True` keeps ties in their original order; reversing afterwards flips them", "`reversed` is faster", "`reverse=True` is unstable"], "answer": 1, "why": "Which is the reason to prefer `reverse=True` in a multi-level sort."}, {"n": 9, "q": "Sorting needs which method?", "options": ["`__eq__`", "`__lt__`", "`__cmp__`", "`__hash__`"], "answer": 1, "why": "`functools.total_ordering` fills in the rest from `__eq__` and `__lt__`; a dataclass with `order=True` generates all of them."}, {"n": 10, "q": "`sorted([\"banana\", \"Apple\"])` gives", "options": ["`[\"Apple\", \"banana\"]`, because capitals sort before lowercase", "`[\"banana\", \"Apple\"]`", "A TypeError", "Locale-dependent output"], "answer": 0, "why": "String comparison is by code point. `key=str.lower` fixes the common case and `str.casefold` handles more."}, {"n": 11, "q": "Sorting records where some dates are `None`", "options": ["Works; `None` sorts first", "Raises TypeError, because there is no ordering between `None` and a date", "Silently drops them", "Sorts them last"], "answer": 1, "why": "The idiom is a tuple key beginning with a flag: `(r.date is None, r.date)`, since `False` sorts before `True`."}, {"n": 12, "q": "To get the three largest of a million items", "options": ["`sorted(items)[-3:]`", "`heapq.nlargest(3, items)`", "`max(items)` three times", "`items.sort()` then slice"], "answer": 1, "why": "Sorting everything is n log n work for a question that needs one pass and a heap of three."}, {"n": 13, "q": "`bisect.insort` is for", "options": ["Sorting faster", "Inserting into an already-sorted list at the right position", "Splitting a list in two", "Binary comparison of two lists"], "answer": 1, "why": "A search per insert rather than a sort per query, which is the right trade when reads outnumber writes."}, {"n": 14, "q": "Timsort on already-sorted input is", "options": ["n log n, like any other input", "Roughly linear, because it finds the existing run", "Slower, because of the run detection", "Undefined"], "answer": 1, "why": "Which is why re-sorting a list you have added a few items to is much cheaper than sorting from scratch."}, {"n": 15, "q": "Sorting by `hash(x)` produces an order that", "options": ["Is stable across runs", "Changes between processes, because string hashing is randomised", "Is the same as sorting by value", "Raises"], "answer": 1, "why": "Tests that depend on it pass locally and fail in CI. Sort by something the data actually contains."}]