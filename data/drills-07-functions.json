[{"n": 1, "q": "When does a `def` statement's default expression run?", "options": ["On every call that omits the argument", "Once, when the `def` statement itself executes", "On the first call only", "When the function is first inspected"], "answer": 1, "why": "The result is stored on the function object as `f.__defaults__` and shared by every call for the life of the process."}, {"n": 2, "q": "`def stamp(at=time.time())` gives every call", "options": ["The time of that call", "The time the module was imported", "A TypeError", "`None`"], "answer": 1, "why": "A call in a default runs once, at definition time. ruff flags the shape as `B008`."}, {"n": 3, "q": "`def f(a, b=a)` does what?", "options": ["Defaults `b` to whatever `a` was passed", "Raises NameError when the `def` runs", "Defaults `b` to `None`", "Is a SyntaxError"], "answer": 1, "why": "Defaults are evaluated with no call in progress, so no parameter exists yet. Any default depending on the arguments has to be computed in the body."}, {"n": 4, "q": "A bare `*` in a parameter list means", "options": ["Collect surplus positional arguments", "Everything after it is keyword-only", "Everything before it is positional-only", "Unpack the next argument"], "answer": 1, "why": "It costs one character and removes the need to go and read the signature from every call site."}, {"n": 5, "q": "What does `/` mark in a parameter list?", "options": ["Everything before it is positional-only", "Everything after it is keyword-only", "Integer division of the arguments", "The end of required parameters"], "answer": 0, "why": "Which frees you to rename those parameters later, since no caller can depend on the name. Most builtins are positional-only for that reason."}, {"n": 6, "q": "`def total(*values)` called as `total([1, 2, 3])` receives", "options": ["Three arguments", "One argument, which is a list", "A TypeError at the call", "An empty tuple"], "answer": 1, "why": "`*` collects in a definition and spreads at a call site. `total(*[1, 2, 3])` is the spreading form."}, {"n": 7, "q": "Inside a function, `kwargs` is", "options": ["A view onto the caller's keyword arguments", "A new dict, built fresh on every call", "A tuple", "The same dict on every call"], "answer": 1, "why": "Mutating it cannot affect the caller. The positional counterpart, `args`, is a tuple."}, {"n": 8, "q": "`f(x=2, 1)` is", "options": ["Legal; the 1 fills the first free parameter", "A SyntaxError", "A TypeError at runtime", "Legal only with `*args`"], "answer": 1, "why": "Positional arguments must precede keyword arguments, or the reader would have to count to know what `1` fills."}, {"n": 9, "q": "A function that falls off the end returns", "options": ["Nothing at all", "`None`", "The last expression evaluated", "It raises"], "answer": 1, "why": "Which is why a forgotten `return` surfaces as a `None` somewhere else entirely rather than as an error at the mistake."}, {"n": 10, "q": "`first, last = full.split()` where `full` has three words", "options": ["Binds the first two words", "Binds the first and last words", "Raises ValueError", "Binds `last` to a list of the rest"], "answer": 2, "why": "Unpacking checks the count and names both numbers. A starred target, `first, *rest`, is how you absorb the surplus."}, {"n": 11, "q": "A wrapper that must accept anything is defined as", "options": ["`def wrapper(args, kwargs):`", "`def wrapper(*args, **kwargs):`", "`def wrapper(*args):`", "`def wrapper(**kwargs):`"], "answer": 1, "why": "And it forwards with `original(*args, **kwargs)`, using the same two symbols in their spreading sense."}, {"n": 12, "q": "What do type annotations do at runtime?", "options": ["Reject arguments of the wrong type", "Convert arguments to the annotated type", "Nothing; they are stored in `__annotations__`", "Slow the call down"], "answer": 2, "why": "They are documentation a separate checker can verify. They are also what lets mypy look inside the function at all."}, {"n": 13, "q": "Passing a list to a function that mutates it means", "options": ["The caller sees the change", "The function works on a copy", "A TypeError", "The change is visible only if the function returns it"], "answer": 0, "why": "Parameters are bound to the caller's objects, exactly like any other name. Nothing is copied at the boundary."}, {"n": 14, "q": "Which is the better signature for an option a reader could not identify by position?", "options": ["`def render(t, False, 4)`", "`def render(t, *, escape=True, indent=2)`", "`def render(t, escape, indent)`", "`def render(t, **opts)`"], "answer": 1, "why": "The bare `*` makes `render(t, escape=False, indent=4)` the only legal call, so a call site three files away reads without a lookup."}, {"n": 15, "q": "A docstring is", "options": ["A comment the interpreter discards", "A string literal stored as `__doc__`", "Required on every function", "Only used by linters"], "answer": 1, "why": "It is what `help()` prints and what an editor shows on hover, and unit 20 makes the examples inside it run as tests."}]