[{"n": 1, "q": "`p.x` is shorthand for", "options": ["`type(p).__getattribute__(p, \"x\")`", "`p.__dict__[\"x\"]`", "`type(p).__dict__[\"x\"]`", "`p.__getattr__(\"x\")`"], "answer": 0, "why": "Every attribute access in the language goes through that one method."}, {"n": 2, "q": "`__getattr__` is called", "options": ["On every attribute access", "Only after the normal lookup has already failed", "Only for names starting with an underscore", "Before `__getattribute__`"], "answer": 1, "why": "Which is why a class that defines it keeps working normally for names it does have."}, {"n": 3, "q": "`__getattribute__` is called", "options": ["On every attribute access, methods included", "Only when the name is missing", "Only for class attributes", "Only when `__getattr__` is absent"], "answer": 0, "why": "Override it and you own everything, including the parts that were fine."}, {"n": 4, "q": "`vars(p)` gives you", "options": ["`p.__dict__`, holding only what was assigned to `self`", "Everything reachable on `p`, inherited names included", "The class's attributes", "The local variables of the last method called"], "answer": 0, "why": "`dir(p)` is the one that walks the class and its bases."}, {"n": 5, "q": "A data descriptor on the class, against an entry in the instance dict", "options": ["The descriptor wins", "The instance dict wins", "Whichever was set last", "Raises `AttributeError`"], "answer": 0, "why": "Which is how a `property` intercepts a read even when the instance dict has that name."}, {"n": 6, "q": "`getattr(p, \"x\", 0)`", "options": ["Returns `0` instead of raising when `x` is missing", "Sets `x` to `0`", "Raises unless `x` is `0`", "Is the same as `p.x or 0`"], "answer": 0, "why": "And it is clearer than `hasattr` followed by an access, as well as shorter."}, {"n": 7, "q": "`hasattr(obj, \"name\")` returns `False` when", "options": ["The attribute is missing", "Anything in the lookup raises `AttributeError`, the attribute's own body included", "The attribute is `None`", "The attribute is falsy"], "answer": 1, "why": "Which is how a typo inside a property gets reported to the caller as \"not there\"."}, {"n": 8, "q": "A setter for `x` that assigns `self.x`", "options": ["Stores the value", "Calls itself until the stack runs out", "Raises `AttributeError`", "Bypasses the property"], "answer": 1, "why": "The stored value needs its own name, conventionally `self._x`."}, {"n": 9, "q": "`@area.setter` works because", "options": ["`area` is already a property object, and `.setter` returns a new one with the setter attached", "`setter` is a keyword", "Python matches decorators by name", "The getter and setter are the same function"], "answer": 0, "why": "Which is why the name is repeated and why the getter has to come first."}, {"n": 10, "q": "A property with a getter and no setter gives you", "options": ["A read-only attribute; assigning raises `AttributeError`", "A writable attribute", "A class attribute", "A slot"], "answer": 0, "why": "The cheapest way to say \"derived, do not assign\"."}, {"n": 11, "q": "The best argument for starting with a plain attribute", "options": ["Properties are slow", "A plain attribute can become a property later without changing a single call site", "Properties cannot be inherited", "Plain attributes are private"], "answer": 1, "why": "Which is why translating Java-style getter and setter pairs into properties is wasted work."}, {"n": 12, "q": "`functools.cached_property` stores its result", "options": ["In the instance `__dict__`, under the same name, where later reads find it first", "In a module-level dict keyed by the instance", "On the class", "Nowhere; it recomputes"], "answer": 0, "why": "A non-data descriptor, so the instance dict wins over it. That is the whole mechanism."}, {"n": 13, "q": "`__slots__` buys you", "options": ["Less memory per instance, and an error instead of a silent typo", "Private attributes", "Immutable instances", "Type checking"], "answer": 0, "why": "A memory optimisation, so it earns its place when the objects are many and small."}, {"n": 14, "q": "A subclass of a slotted class that declares no `__slots__`", "options": ["Inherits the restriction", "Gets a `__dict__` back, and with it the memory and the typos", "Fails at class creation", "Shares the base class's slots"], "answer": 1, "why": "Every class in the hierarchy has to say so, and each lists only what it adds."}, {"n": 15, "q": "Inside `__setattr__`, storing a value", "options": ["`self.name = value`", "`object.__setattr__(self, name, value)`", "`self.__setattr__(name, value)`", "`setattr(self, name, value)`"], "answer": 1, "why": "`self.__dict__[name] = value` also works, until the class uses `__slots__` and has no dict."}]