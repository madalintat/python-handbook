[{"n": 1, "q": "`pytest` shows the values in a failed `assert` because", "options": ["It rewrites the AST of test modules at import to record the sub-expressions", "It inspects the stack frame", "`assert` does that anyway", "It re-runs the expression"], "answer": 0, "why": "Unit 28's material doing something you will use every day, and why `assertEqual` was ever necessary."}, {"n": 2, "q": "`assert (a == b, \"message\")`", "options": ["Asserts a non-empty tuple, which is always true", "Asserts the comparison with a message", "Is a syntax error", "Asserts both elements"], "answer": 0, "why": "The correct spelling has no brackets. ruff's `F631` catches it."}, {"n": 3, "q": "A test you have never watched fail", "options": ["Is a test you have not verified", "Is a passing test", "Is fine if the code is correct", "Should be deleted"], "answer": 0, "why": "Break the code, confirm the test notices, put it back. Ten seconds."}, {"n": 4, "q": "A `try`/`except ValueError`/`pass` written to check that something raises", "options": ["Passes when nothing raises, which is the opposite of what it meant", "Fails when nothing raises", "Is what `pytest.raises` does", "Cannot compile"], "answer": 0, "why": "`else: raise AssertionError(...)` is the plain fix, and `pytest.raises` does it for you."}, {"n": 5, "q": "`pytest.raises(ValueError, match=\"positive\")` adds", "options": ["A check on the message, so a different `ValueError` from deeper in does not pass", "A retry", "A timeout", "Type narrowing"], "answer": 0, "why": "Worth including, because asserting only the type is weaker than it looks."}, {"n": 6, "q": "Comparing two floats with `==` in a test", "options": ["Is a coin toss; use a tolerance or `pytest.approx`", "Is fine for simple arithmetic", "Works if you round first", "Raises"], "answer": 0, "why": "A test that fails against correct code is worse than one that passes against wrong code: it teaches people to ignore the suite."}, {"n": 7, "q": "`pytest` matches a fixture to a test by", "options": ["The test's parameter name", "A decorator on the test", "Import order", "A registry in `conftest.py`"], "answer": 0, "why": "Which is the piece that looks like magic until you know it."}, {"n": 8, "q": "A fixture that `yield`s", "options": ["Runs the part after the yield as teardown, whether the test passed or failed", "Must be called twice", "Cannot have teardown", "Is a generator the test iterates"], "answer": 0, "why": "Unit 22's context manager protocol again, and unit 16's rule about what `yield` makes a function."}, {"n": 9, "q": "`scope=\"session\"` on a fixture", "options": ["Reuses one object across tests, which makes them order-dependent if anything mutates it", "Runs it once per test", "Runs it in a subprocess", "Is required for slow fixtures"], "answer": 0, "why": "Default to fresh per test; widen only for something genuinely expensive that nothing changes."}, {"n": 10, "q": "`@pytest.mark.parametrize` against a loop inside one test", "options": ["Each case is a separate test with its own name, so one run reports every failure", "It is faster", "It is the same thing", "It runs cases in parallel"], "answer": 0, "why": "A loop of assertions stops at the first failure and tells you less."}, {"n": 11, "q": "`@pytest.mark.xfail`", "options": ["Records a known failure without breaking the suite, and tells you when it starts passing", "Skips the test", "Deletes the test", "Marks a test as slow"], "answer": 0, "why": "The honest way to commit a failing test for a known bug."}, {"n": 12, "q": "Patch a name", "options": ["Where it is used, because the module under test holds its own reference", "Where it is defined", "In both places", "In `conftest.py`"], "answer": 0, "why": "Unit 29's `from x import y` fact, which is why `mock.patch` targets look wrong the first time."}, {"n": 13, "q": "Mocking your own internal functions", "options": ["Asserts that your code calls your code, which is true by construction", "Isolates the unit under test", "Is the point of unit testing", "Is faster than the real thing"], "answer": 0, "why": "Mock the boundary. The tell is a test that is rewritten on every refactor and has never caught a defect."}, {"n": 14, "q": "Coverage is useful for", "options": ["Finding code no test touches at all", "Measuring test quality", "Setting a team target", "Deciding what to refactor"], "answer": 0, "why": "A line that ran is not a line that was checked. Look at what is uncovered; do not chase a number."}, {"n": 15, "q": "A test suite is an asset when it is", "options": ["Fast enough to run on every save, and trustworthy enough that a failure means something", "Complete", "At 100% coverage", "Written before the code"], "answer": 0, "why": "Both properties are lost the same way: by adding tests that do not earn their place."}]