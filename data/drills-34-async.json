[{"n": 1, "q": "A coroutine is", "options": ["A generator that yields when it would otherwise wait", "A thread the interpreter manages", "A process with a shared heap", "A callback registered with the loop"], "answer": 0, "why": "The event loop is a `while` loop that keeps sending values back into whichever one is ready."}, {"n": 2, "q": "Calling an `async def`", "options": ["Returns a coroutine object and runs none of the body", "Runs the body to the first `await`", "Schedules it on the loop", "Runs it to completion"], "answer": 0, "why": "Unit 16's rule about generators, in different words and for the same reason."}, {"n": 3, "q": "`await` means", "options": ["I am not ready; run something else and come back", "Wait here until this finishes", "Block this thread", "Start this concurrently"], "answer": 0, "why": "A coroutine that never awaits never gives anything else a turn."}, {"n": 4, "q": "Two `await`s one after the other", "options": ["Are sequential, and take as long as the sum", "Overlap automatically", "Are a syntax error", "Run on separate threads"], "answer": 0, "why": "Nothing runs concurrently until you use `gather` or a task group."}, {"n": 5, "q": "`asyncio.gather` returns results", "options": ["In the order you passed the coroutines, not the order they finished", "In completion order", "As they arrive", "Unordered"], "answer": 0, "why": "Which is the right default, and wrong when you want the fast answers first."}, {"n": 6, "q": "When one task in a `TaskGroup` raises, the others are", "options": ["Cancelled, and an `ExceptionGroup` carries every failure", "Left to run, and the first exception is returned", "Left to run, and all exceptions are returned", "Retried"], "answer": 0, "why": "`gather`'s default is the second, which discards the rest. `except*` matches inside a group."}, {"n": 7, "q": "Structured concurrency means", "options": ["Tasks cannot outlive the block that created them", "Tasks run in a fixed order", "Each task has its own loop", "Exceptions are grouped"], "answer": 0, "why": "Unit 22's argument for `with`, applied to work rather than to files."}, {"n": 8, "q": "`return` inside an `except*` block", "options": ["Is not allowed, because the block may run once per matching exception type", "Returns the first match", "Is allowed", "Ends the group"], "answer": 0, "why": "Assign to a name and return after the `try`."}, {"n": 9, "q": "A synchronous call inside a coroutine", "options": ["Stops every other coroutine, because the loop is a `while` loop in the same thread", "Runs on a worker thread", "Is scheduled after the current task", "Raises"], "answer": 0, "why": "The failure that makes people conclude async is slow, and it is always this."}, {"n": 10, "q": "A library with no async version is bridged with", "options": ["`await asyncio.to_thread(func, *args)`", "`asyncio.run` inside the coroutine", "`await` on the synchronous call", "A second event loop"], "answer": 0, "why": "And unit 33's rule about the GIL then decides whether a thread is enough."}, {"n": 11, "q": "`asyncio.CancelledError` inherits from", "options": ["`BaseException`, so `except Exception:` does not catch it", "`Exception`", "`RuntimeError`", "`TimeoutError`"], "answer": 0, "why": "Deliberately, which is one more reason a bare `except:` in a coroutine is dangerous."}, {"n": 12, "q": "A task that catches `CancelledError` and returns normally", "options": ["Lies about its own state to whoever cancelled it; re-raise, or clean up in `finally`", "Is correctly handling cancellation", "Is cancelled anyway", "Raises at the next `await`"], "answer": 0, "why": "`finally` needs no catch at all and runs as the exception passes through."}, {"n": 13, "q": "`asyncio.Semaphore(n)`", "options": ["Limits how many things run at once, which is the rate limit", "Limits total tasks", "Orders tasks", "Bounds memory"], "answer": 0, "why": "A bounded `asyncio.Queue` is the other half: it gives a fast producer backpressure."}, {"n": 14, "q": "A task nobody holds a reference to", "options": ["Can be collected mid-flight, and its exception reported late or not at all", "Runs to completion regardless", "Is cancelled at once", "Raises immediately"], "answer": 0, "why": "The strongest argument for using a `TaskGroup` by default."}, {"n": 15, "q": "An `async def` containing `yield`", "options": ["Is an async generator, iterated with `async for` via `__aiter__` and `__anext__`", "Is a syntax error", "Is an ordinary generator", "Returns a coroutine"], "answer": 0, "why": "Every protocol from unit 22 has an async twin, spelled the same way with an `a`."}]