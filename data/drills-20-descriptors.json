[{"n": 1, "q": "A descriptor is any object that defines", "options": ["`__get__`, and optionally `__set__` and `__delete__`", "`__getattr__`", "`__getitem__`", "`__call__`"], "answer": 0, "why": "Found on a class, Python calls it instead of handing it to you."}, {"n": 2, "q": "In `__get__(self, obj, objtype)`, `obj` is", "options": ["The instance the access came through, or `None` when it came through the class", "The descriptor", "The class", "The value being read"], "answer": 0, "why": "Which is why `Thing.x` and `thing.x` can mean different things."}, {"n": 3, "q": "A data descriptor is one that defines", "options": ["`__set__` or `__delete__`, and it wins over the instance dict", "`__get__` only, and the instance dict wins over it", "`__set_name__`", "`__init__`"], "answer": 0, "why": "One asymmetry, and it explains both `property` and `cached_property`."}, {"n": 4, "q": "`cached_property` works because it is", "options": ["A non-data descriptor, so the value it writes into the instance dict wins from then on", "A data descriptor with a cache inside it", "A class attribute", "A `property` with memoisation"], "answer": 0, "why": "A `property` cannot cache itself this way, because defining `__set__` puts it ahead of the dict."}, {"n": 5, "q": "A plain function stored on a class is", "options": ["A non-data descriptor whose `__get__` returns a bound method", "A special case in the interpreter", "A data descriptor", "Not a descriptor at all"], "answer": 0, "why": "Which is the entire mechanism behind `self`."}, {"n": 6, "q": "`classmethod.__get__` differs from a function's in that it binds", "options": ["Nothing", "`objtype` rather than `obj`", "Both", "The module"], "answer": 1, "why": "One difference, and it is the whole reason `cls` is the class you called it on."}, {"n": 7, "q": "`staticmethod.__get__` returns", "options": ["The function, with nothing bound", "A method bound to the instance", "A method bound to the class", "The descriptor"], "answer": 0, "why": "Which is why it takes no implicit first argument."}, {"n": 8, "q": "Each name in `__slots__` becomes", "options": ["A data descriptor on the class, reading and writing a fixed position", "An entry in the instance dict", "A property", "A type annotation"], "answer": 0, "why": "Which is why slots beat the instance dict, and why there is no dict to beat."}, {"n": 9, "q": "`__set_name__(self, owner, name)` is called", "options": ["Once, by Python, when the class body finishes", "On every access", "On every assignment", "When the instance is created"], "answer": 0, "why": "Which is how a descriptor learns the name it was bound to without being told twice."}, {"n": 10, "q": "A descriptor assigned to an **instance** rather than a class", "options": ["Works the same way", "Is an ordinary value; the protocol never fires", "Raises `TypeError`", "Shadows the class's"], "answer": 1, "why": "The lookup searches the type for a descriptor and the instance dict for a value."}, {"n": 11, "q": "`self.value = value` inside `__set__`", "options": ["Stores per instance", "Stores one value shared by every instance of the owning class", "Raises", "Stores on the class"], "answer": 1, "why": "The descriptor is created once in the class body. Unit 18's shared class attribute, with more ceremony."}, {"n": 12, "q": "`setattr(obj, self.name, value)` inside `__set__`, where `name` is the bound name", "options": ["Writes to the instance dict", "Calls `__set__` again, forever", "Writes to the class", "Raises `AttributeError`"], "answer": 1, "why": "The third time this has come up: code that handles attribute access must not reach for the attribute it handles."}, {"n": 13, "q": "`if obj is None: return self` at the top of `__get__`", "options": ["Handles access through the class, and returning the descriptor is the useful answer", "Guards against uninitialised instances", "Is optional decoration", "Makes the descriptor read only"], "answer": 0, "why": "It is what `property` does, and what lets tooling find the descriptor on the class."}, {"n": 14, "q": "Leaving `__set__` out to make an attribute read only", "options": ["Works", "Makes it overridable instead: an assignment writes past the descriptor into the instance dict", "Raises at class creation", "Makes it a data descriptor"], "answer": 1, "why": "Refusing a write means defining `__set__` and raising from it."}, {"n": 15, "q": "The best reason to write a descriptor rather than a property", "options": ["It is faster", "The same rule applies to several attributes or several classes, and a property would repeat it", "Properties cannot validate", "Properties cannot be inherited"], "answer": 1, "why": "At one field it is questionable. At two it starts paying."}]