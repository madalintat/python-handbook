[{"n": 1, "q": "If you write only one of the two representations, write", "options": ["`__repr__`, because the REPL, tracebacks, lists and debuggers all use it", "`__str__`, because `print` uses it", "Both, always", "`__format__`"], "answer": 0, "why": "A missing `__str__` falls back to `__repr__`. A missing `__repr__` has a default nothing warns you about."}, {"n": 2, "q": "`!r` in an f-string", "options": ["Calls `repr` on the field, so strings come out quoted", "Escapes the value", "Formats it as raw text", "Rounds it"], "answer": 0, "why": "The goal is output that could be pasted back in to rebuild the object."}, {"n": 3, "q": "A binary method that cannot handle the other operand should", "options": ["Return `NotImplemented`, so Python can try the reflected method", "Raise `TypeError`", "Return `None`", "Return `False`"], "answer": 0, "why": "Raising skips the other operand's turn. If nobody can do it, Python raises a better error than yours."}, {"n": 4, "q": "`a + b` where `type(a).__add__` returns `NotImplemented` tries", "options": ["`type(b).__radd__(b, a)`", "`type(a).__radd__(a, b)`", "`type(b).__add__(b, a)`", "Nothing; it raises"], "answer": 0, "why": "Which is how `2 * vector` works when `int` has never heard of your class."}, {"n": 5, "q": "Not defining `__iadd__`", "options": ["Makes `a += b` fall back to `a = a + b`", "Makes `+=` raise", "Makes `+=` mutate in place", "Has no effect on `+=`"], "answer": 0, "why": "Defining it is how a mutable container makes `+=` mutate rather than rebind."}, {"n": 6, "q": "`functools.total_ordering` fills in the comparisons from", "options": ["`__eq__` and one of the ordering methods", "`__lt__` alone", "`__hash__`", "`__repr__`"], "answer": 0, "why": "Saving four nearly identical methods. `__eq__` without `__hash__` still makes the object unhashable."}, {"n": 7, "q": "An object with `__len__` and no `__bool__` is falsy when", "options": ["Its length is zero", "It is `None`", "Always", "Never"], "answer": 0, "why": "Which is wrong for anything where \"empty\" and \"absent\" are different questions."}, {"n": 8, "q": "`x[1:3]` arrives at `__getitem__` as", "options": ["A `slice` object", "Two arguments", "A tuple of ints", "A `range`"], "answer": 0, "why": "`__getitem__` receives whatever was between the brackets, whatever that was."}, {"n": 9, "q": "Without `__contains__`, `in`", "options": ["Falls back to iterating and comparing, which is correct and linear", "Raises `TypeError`", "Returns `False`", "Uses `__getitem__` only"], "answer": 0, "why": "So define it when you can answer faster than a walk."}, {"n": 10, "q": "`with expr as name` binds", "options": ["Whatever `__enter__` returns", "`expr`", "`None`", "The result of `__exit__`"], "answer": 0, "why": "Which is why most `__enter__` methods return `self`, and why forgetting the `return` gives you `None`."}, {"n": 11, "q": "`__exit__` returning a truthy value", "options": ["Suppresses the exception", "Signals that cleanup succeeded", "Re-raises the exception", "Is ignored"], "answer": 0, "why": "Almost never what a cleanup method meant. Return nothing."}, {"n": 12, "q": "`__exit__` runs", "options": ["On the way out whatever happens, with the exception if there was one", "Only when the block completes normally", "Only when the block raises", "Before `__enter__` on failure"], "answer": 0, "why": "Which is the whole reason a context manager exists."}, {"n": 13, "q": "A `@contextmanager` function needs `try`/`finally` because", "options": ["An exception in the body is thrown back in at the `yield`, skipping everything after it", "The decorator requires it", "`yield` cannot appear alone", "It makes the generator reusable"], "answer": 0, "why": "The standard bug: works for a year, leaks the first time something fails."}, {"n": 14, "q": "`len(x)` looks for `__len__` on", "options": ["`type(x)`, never the instance", "`x`, then `type(x)`", "`x.__dict__`", "The module"], "answer": 0, "why": "True of every implicit dunder, which is why a `__getattr__` that answers everything cannot accidentally make an object callable."}, {"n": 15, "q": "The test for whether to implement an operator is", "options": ["Whether a reader who has never seen the class would predict what it does", "Whether it saves typing", "Whether the operation is common", "Whether the type is a container"], "answer": 0, "why": "If you need a docstring to explain what your `+` means, the operation wanted a name."}]