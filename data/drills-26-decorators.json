[{"n": 1, "q": "`@log` above `def fetch` means", "options": ["`fetch = log(fetch)`", "`log` is called each time `fetch` is called", "`fetch` inherits from `log`", "`log` is registered as a hook"], "answer": 0, "why": "Every property of decorators follows from that one line."}, {"n": 2, "q": "Decorators run", "options": ["At definition time, which for a module-level function means at import", "At each call", "At the first call", "When the module is unloaded"], "answer": 0, "why": "Which is exactly what registration decorators rely on."}, {"n": 3, "q": "Stacked decorators apply", "options": ["Bottom up: the one nearest the `def` wraps the function first", "Top down", "In an unspecified order", "Alphabetically"], "answer": 0, "why": "So the one at the top is outermost and sees the call first."}, {"n": 4, "q": "Something that can reject a call belongs", "options": ["At the top of the stack, where nothing below can answer first", "Nearest the `def`", "Inside the function", "Anywhere; order does not matter"], "answer": 0, "why": "Otherwise a cache below it serves the request the check would have refused."}, {"n": 5, "q": "The most common decorator bug is", "options": ["A wrapper that calls the function and does not return its result", "A missing `functools.wraps`", "Wrong stacking order", "Using a class"], "answer": 0, "why": "Quiet in the worst way: functions used for effects keep working, functions used for values return `None`."}, {"n": 6, "q": "Without `functools.wraps`, the decorated function", "options": ["Has the wrapper's `__name__`, no docstring, and a `(*args, **kwargs)` signature", "Cannot be called", "Loses its arguments", "Is slower"], "answer": 0, "why": "Which breaks `help`, Sphinx, pytest names, tracebacks and anything dispatching on the name."}, {"n": 7, "q": "`functools.wraps` also sets", "options": ["`__wrapped__`, so the original stays reachable", "`__slots__`", "`__call__`", "`__module__` only"], "answer": 0, "why": "`inspect.unwrap` follows that chain when there are several layers."}, {"n": 8, "q": "`@retry(times=3)` expands to", "options": ["`f = retry(times=3)(f)`", "`f = retry(f, times=3)`", "`f = retry(times=3, f)`", "`retry(f).times = 3`"], "answer": 0, "why": "Two calls, which is why a decorator with arguments needs three levels."}, {"n": 9, "q": "`@thing` where `@thing()` was needed", "options": ["Passes the function in as the first configuration argument, failing somewhere downstream", "Raises at the decoration", "Uses the defaults", "Is equivalent"], "answer": 0, "why": "The usual answer is to require the parentheses and let the failure be loud."}, {"n": 10, "q": "`@retry(SETTINGS[\"attempts\"])` reads the setting", "options": ["Once, when the module is imported", "On every call", "On the first call", "Whenever `SETTINGS` changes"], "answer": 0, "why": "Unit 07's mutable-default surprise in a new setting: an expression in a position that runs once."}, {"n": 11, "q": "A class-based decorator on a **method**", "options": ["Breaks, because an instance is not a descriptor and nothing binds `self`", "Works exactly as on a function", "Requires `@staticmethod`", "Cannot be written"], "answer": 0, "why": "Unit 20's protocol arriving where you would not expect it. A closure is a function, and functions are descriptors."}, {"n": 12, "q": "For a class-based decorator, the metadata is copied with", "options": ["`functools.update_wrapper(self, func)`", "`functools.wraps`", "`copy.copy`", "Nothing; it is automatic"], "answer": 0, "why": "`wraps` is the decorator form of the same thing, for wrapping through a function."}, {"n": 13, "q": "A registration decorator such as `@app.route(\"/\")`", "options": ["Puts the function in a table and returns it unchanged", "Wraps it in a request handler", "Replaces it with a route object", "Defers it until the server starts"], "answer": 0, "why": "The most common kind in real code and the least like the tutorial shape. It still has to return the function."}, {"n": 14, "q": "Library decorators use `ParamSpec` to", "options": ["Say \"the same parameters as the function I wrapped\", so annotations survive the wrapper", "Validate arguments", "Accept both call forms", "Speed up dispatch"], "answer": 0, "why": "A bare `*args, **kwargs` erases the signature as far as a checker is concerned."}, {"n": 15, "q": "The test for whether behaviour belongs in a decorator is", "options": ["Whether a reader looking only at the call site would be surprised", "Whether it is reusable", "Whether it fits in one function", "Whether the standard library has one"], "answer": 0, "why": "`@cache` passes. A decorator that raises for reasons the function's body does not contain does not."}]