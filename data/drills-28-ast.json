[{"n": 1, "q": "The three modules answer", "options": ["`inspect` what an object is, `dis` what the interpreter will do, `ast` what the source says", "The same question at different speeds", "`inspect` for classes, `dis` for functions, `ast` for modules", "Debugging, profiling and linting"], "answer": 0, "why": "Different levels: objects, bytecode, and syntax before anything runs."}, {"n": 2, "q": "`inspect.signature(func).parameters` gives you", "options": ["An ordered mapping of `Parameter` objects, each with a name, kind, default and annotation", "A list of names", "The argument values of the last call", "The annotations only"], "answer": 0, "why": "Which is how every CLI generator and dependency injector works out what to pass."}, {"n": 3, "q": "`inspect.signature` on a function decorated without `functools.wraps` reports", "options": ["`(*args, **kwargs)`, because the decorated function genuinely is the wrapper", "The original signature", "Nothing", "An error"], "answer": 0, "why": "It follows `__wrapped__`, and `wraps` is what sets it."}, {"n": 4, "q": "`inspect.getsource` fails for", "options": ["Anything defined in a REPL, in an `exec`, or inside a zipped package", "Decorated functions", "Classes", "Anything in the standard library"], "answer": 0, "why": "It reads the file, so there has to be a file."}, {"n": 5, "q": "`dis` is good for", "options": ["Settling what a construct compiles to, and what a closure captured", "Optimising", "Finding type errors", "Measuring run time"], "answer": 0, "why": "Instruction count is a poor proxy for time, and the compiler changes between versions."}, {"n": 6, "q": "`ast.NodeVisitor` dispatches to", "options": ["`visit_<NodeType>` if you defined one, and `generic_visit` otherwise", "`visit` for every node", "Whatever `__match_args__` says", "Methods in definition order"], "answer": 0, "why": "Which is why a `visit_X` replaces the descent rather than adding to it."}, {"n": 7, "q": "A `visit_X` that does not call `generic_visit`", "options": ["Stops the walk there, so nothing nested inside is ever visited", "Visits children twice", "Raises", "Is the normal case"], "answer": 0, "why": "Every mysteriously incomplete AST tool is this."}, {"n": 8, "q": "In a `NodeTransformer`, returning `None` from a visit method", "options": ["Deletes the node", "Leaves it unchanged", "Raises", "Replaces it with `None`"], "answer": 0, "why": "Returning a node replaces it, and returning a list splices several in."}, {"n": 9, "q": "After building nodes by hand, `compile` needs", "options": ["`ast.fix_missing_locations(tree)`, because constructed nodes have no line or column", "`ast.dump`", "`ast.unparse`", "Nothing extra"], "answer": 0, "why": "It copies positions down from each node's parent, so it is called once on the finished tree."}, {"n": 10, "q": "Numeric and string literals are", "options": ["`ast.Constant` with a `value`; `Num` and `Str` were removed in 3.12", "`ast.Num` and `ast.Str`", "`ast.Literal`", "`ast.Value`"], "answer": 0, "why": "While the shims existed, `isinstance(node, ast.Num)` matched nothing and said nothing."}, {"n": 11, "q": "`Name` nodes distinguish a read from a write by", "options": ["`ctx`, which is `Load`, `Store` or `Del`", "The node type", "Their position in the parent", "`lineno`"], "answer": 0, "why": "The same field covers comprehension targets, `for` targets and `with ... as`."}, {"n": 12, "q": "The shortest way to find out what a node looks like is", "options": ["`ast.dump(ast.parse(source), indent=2)` on an example", "Reading the grammar", "`dir(ast)`", "`help(ast.Assign)`"], "answer": 0, "why": "Faster than the documentation for anything but the simplest node."}, {"n": 13, "q": "`ast.unparse`", "options": ["Turns a tree back into source, normalised, with comments and formatting gone", "Reproduces the original text exactly", "Requires the original source", "Is a compile step"], "answer": 0, "why": "A refactoring tool that must leave the file otherwise byte-identical needs LibCST instead."}, {"n": 14, "q": "`ast.literal_eval` against `eval`, on input from a file or a form", "options": ["`literal_eval` parses a literal and refuses calls, names and imports; `eval` runs anything", "They are equivalent for data", "`literal_eval` is slower but identical", "`eval` is safe with a restricted namespace"], "answer": 0, "why": "If `literal_eval` refuses your input, the input was not data."}, {"n": 15, "q": "The line between using introspection well and badly is", "options": ["Inspecting an object, against depending on who called you", "Reading against writing", "`inspect` against `ast`", "Import time against call time"], "answer": 0, "why": "Reading a signature to build a CLI is inspection. Dispatching on `inspect.stack()` makes every caller part of the contract."}]