[{"n": 1, "q": "An object whose reference count reaches zero is", "options": ["Freed immediately", "Freed at the next collection", "Marked for collection", "Freed when memory runs low"], "answer": 0, "why": "Which is why a file whose last reference disappears is closed promptly."}, {"n": 2, "q": "`sys.getrefcount(obj)` reads one higher than expected because", "options": ["Passing the object to the function is itself a reference", "It counts the object itself", "It counts weak references too", "It rounds up"], "answer": 0, "why": "Every reference counts, including the temporary ones you did not name."}, {"n": 3, "q": "Two objects that reference each other", "options": ["Cannot be freed by reference counting, and wait for the cycle collector", "Are freed normally", "Leak permanently", "Are refused at creation"], "answer": 0, "why": "So the memory sits held until the collector happens to run."}, {"n": 4, "q": "`__del__` is a poor place for cleanup because", "options": ["You cannot say when it runs, and any exception in it is swallowed", "It is never called", "It prevents collection", "It is deprecated"], "answer": 0, "why": "Use `with` and a `close` method."}, {"n": 5, "q": "`sys.getsizeof(a_list)` reports", "options": ["The list's own size, the pointers, and not what they point at", "The whole graph", "The number of elements", "The allocated arena"], "answer": 0, "why": "A list of a million small integers is around forty megabytes, not eight."}, {"n": 6, "q": "`b = a` where `a` is a large list costs", "options": ["One pointer; assignment never copies", "A copy of the list", "A copy of everything in it", "Nothing, because lists are shared"], "answer": 0, "why": "`a[:]` copies the container; `deepcopy(a)` copies everything reachable."}, {"n": 7, "q": "`a[:]` on a list of dicts gives you", "options": ["A new list holding the very same dicts", "A new list holding copies of the dicts", "The same list", "A read-only view"], "answer": 0, "why": "Isolation from changes to the list, not from changes to the rows."}, {"n": 8, "q": "Small integers from -5 to 256", "options": ["Are created once at startup and shared, which is why `is` sometimes appears to work", "Are compared by value by `is`", "Are interned only in the REPL", "Are not objects"], "answer": 0, "why": "Compare with `==`, and keep `is` for `None`, `True` and `False`."}, {"n": 9, "q": "The commonest memory leak in Python is", "options": ["A module-level container that only grows", "A reference cycle", "A missing `__del__`", "An unclosed file"], "answer": 0, "why": "It does not look like a leak. It looks like a cache, which is a thing you were supposed to add."}, {"n": 10, "q": "`functools.cache` against `lru_cache(maxsize=128)`", "options": ["`cache` has no bound at all, which is a leak for anything driven by user input", "They are the same", "`cache` evicts by age", "`lru_cache` is deprecated"], "answer": 0, "why": "Choosing between them is choosing whether the set of keys is bounded by something other than time."}, {"n": 11, "q": "`weakref.WeakSet` differs from a list in that", "options": ["It does not keep its members alive, and entries disappear when they are collected", "It is faster", "It deduplicates", "It is thread-safe"], "answer": 0, "why": "`int`, `str`, `tuple` and `list` cannot be referenced weakly, and `__slots__` needs `__weakref__` in the list."}, {"n": 12, "q": "The single biggest memory win for a class you make a great many of is", "options": ["`__slots__`, which removes the per-instance dict", "`frozen=True`", "A metaclass", "Caching instances"], "answer": 0, "why": "Typically halves it, and turns an undeclared attribute into an immediate error."}, {"n": 13, "q": "A process that peaks at two gigabytes and frees everything usually", "options": ["Still shows two gigabytes, because arenas are returned only when entirely free", "Returns to its starting size", "Has a leak", "Has fragmented the heap beyond use"], "answer": 0, "why": "Resident memory measures the high-water mark. `tracemalloc` measures what Python holds."}, {"n": 14, "q": "To find a leak, the method that works is", "options": ["Snapshot, do a unit of work, snapshot again, compare", "Take one snapshot and read the largest entries", "Call `gc.collect()` and watch resident memory", "Count objects by type"], "answer": 0, "why": "Anything that grows every time round is the leak, and `compare_to` names its allocation site."}, {"n": 15, "q": "For a long-running server, the property to check is", "options": ["Whether memory is flat after warm-up, not what the number is", "That it stays under a threshold", "That `gc.collect()` reclaims memory", "That there are no cycles"], "answer": 0, "why": "A slow steady climb over a day is a leak whatever the number is."}]