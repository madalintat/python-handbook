[{"n": 1, "q": "A class body", "options": ["Runs once, at import, and its names become the class's `__dict__`", "Runs each time an instance is made", "Is only a declaration and does not execute", "Runs when a method is first called"], "answer": 0, "why": "Which is the whole explanation for a shared mutable class attribute."}, {"n": 2, "q": "Which method creates the object?", "options": ["`__init__`", "`__new__`", "`__call__`", "`__create__`"], "answer": 1, "why": "`__init__` receives the already-made object as `self` and sets it up, which is why it is an initialiser rather than a constructor."}, {"n": 3, "q": "`__init__` returning a value", "options": ["Is how you return the instance", "Raises TypeError; it must return None", "Is ignored", "Replaces the instance"], "answer": 1, "why": "Enforced rather than ignored, which is worth being glad about: a returned `self` would look like it worked."}, {"n": 4, "q": "Reading `self.x` looks", "options": ["In the instance first, then the class", "In the class first, then the instance", "Only in the instance", "In the module namespace"], "answer": 0, "why": "And writing `self.x` always goes to the instance, which is the asymmetry every surprise in this unit comes from."}, {"n": 5, "q": "`self.count += 1` where `count` is a class attribute", "options": ["Increments the class attribute", "Reads the class's value and writes a new instance attribute", "Raises AttributeError", "Increments both"], "answer": 1, "why": "Name the class to change the class's: `Widget.count += 1`."}, {"n": 6, "q": "`self.items.append(x)` where `items` is a class attribute", "options": ["Creates an instance attribute", "Mutates the one shared list, because a read finds the class's", "Raises", "Copies the list first"], "answer": 1, "why": "Assignment shadows; mutation does not. Per-instance state has to be created in `__init__`."}, {"n": 7, "q": "`p.method` where `method` is defined in the class gives", "options": ["The plain function", "A bound method, with the instance already supplied as the first argument", "A copy of the function", "An error unless called"], "answer": 1, "why": "`Point.method` gives the plain function, which is why calling it on the class needs an explicit instance."}, {"n": 8, "q": "An alternative constructor should be a", "options": ["`staticmethod`", "`classmethod`, so `cls(...)` builds whatever class it was called on", "plain method", "module function"], "answer": 1, "why": "A `staticmethod` has to name a class, and the name it picks is wrong for every subclass."}, {"n": 9, "q": "A `staticmethod` receives", "options": ["The instance", "The class", "Nothing extra", "The metaclass"], "answer": 2, "why": "If nothing outside the class would ever want it, a `staticmethod` says so; otherwise a module function is simpler."}, {"n": 10, "q": "Two instances with the same attribute values, and no `__eq__`", "options": ["Compare equal", "Compare unequal, because the default compares identity", "Raise TypeError", "Compare equal only if hashable"], "answer": 1, "why": "Right for things with an identity; wrong for things that are values."}, {"n": 11, "q": "An instance's `__dict__` holds", "options": ["Only what was assigned to `self`", "Its methods too", "Everything inherited", "The class attributes as well"], "answer": 0, "why": "`dir(p)` is the one that lists everything reachable, including inherited names."}, {"n": 12, "q": "`type(Point)` where `Point` is a class is", "options": ["`object`", "`type`", "`Point`", "`class`"], "answer": 1, "why": "A class is an object made by a class, which is unit 27's subject."}, {"n": 13, "q": "A double leading underscore, `__total`, gives you", "options": ["Privacy", "Name mangling, to stop a subclass colliding with a base class's attribute", "A read-only attribute", "A class attribute"], "answer": 1, "why": "Python has no private attributes. A single underscore is a message to a reader and nothing more."}, {"n": 14, "q": "`__new__` that returns nothing gives you", "options": ["An empty instance", "`None` from calling the class, with no complaint at the mistake", "A TypeError", "The class itself"], "answer": 1, "why": "And `__init__` is only called when what came back is an instance of the class."}, {"n": 15, "q": "A class whose every method takes the same arguments and stores nothing is", "options": ["Well designed", "A set of functions wearing a costume", "A singleton", "A dataclass"], "answer": 1, "why": "If there is no state several functions share, a module of functions is simpler."}]