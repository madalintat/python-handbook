[{"n": 1, "q": "Calling a function that contains `yield`", "options": ["Runs the body and returns the values", "Runs none of the body and returns a generator", "Raises unless you use `next`", "Runs up to the first `yield`"], "answer": 1, "why": "The body starts only when something asks for the first value, which is why validation written inside a generator happens late."}, {"n": 2, "q": "`yield` differs from `return` in that", "options": ["It suspends the function with all its local state intact, to be resumed", "It is faster", "It can only appear once", "It converts the value to a string"], "answer": 0, "why": "The locals, the position in the loop and the point inside a `try` are all still there when it resumes."}, {"n": 3, "q": "`return value` inside a generator", "options": ["Yields the value last", "Ends the generator, attaching the value to `StopIteration`", "Is a SyntaxError", "Restarts the generator"], "answer": 1, "why": "A `for` loop discards it. It exists mainly so `yield from` can pass a result back."}, {"n": 4, "q": "A generator function called twice gives", "options": ["Two independent generators", "The same generator", "A TypeError on the second call", "A copy sharing state"], "answer": 0, "why": "Which is how a class becomes reusably iterable: put the `yield` in `__iter__`."}, {"n": 5, "q": "A `StopIteration` raised inside a generator", "options": ["Ends the generator quietly", "Becomes a RuntimeError, since Python 3.7", "Is caught by the caller's `for` loop", "Is a SyntaxError"], "answer": 1, "why": "Before PEP 479 it escaped and read as \"finished\", so the outer loop stopped early with nothing to find."}, {"n": 6, "q": "`yield from row` differs from `for x in row: yield x` in that", "options": ["It is the only one that works", "It also forwards `send`, `throw` and `close`, and passes back the return value", "It flattens recursively", "It is lazy where the loop is not"], "answer": 1, "why": "For plain iteration they are equivalent, and ruff's `UP028` suggests the shorter one."}, {"n": 7, "q": "`g.send(10)` on a freshly created generator", "options": ["Returns the first value", "Raises TypeError, because there is no paused `yield` to receive it", "Ignores the value", "Starts the generator"], "answer": 1, "why": "Priming it with `next(g)` runs the body up to the first `yield` and leaves it ready to receive."}, {"n": 8, "q": "`n = yield total` means", "options": ["Yield `total`, then when resumed, `n` becomes whatever was sent", "Assign `total` to `n` and yield it", "Yield both", "A SyntaxError"], "answer": 0, "why": "This two-way machinery is what `async`/`await` was built on."}, {"n": 9, "q": "A `finally` around a `yield` runs", "options": ["Immediately after the yield", "When the generator is closed or collected, which may not be at once", "Never", "Only on an exception"], "answer": 1, "why": "Which is why a `with` inside the generator says it better when the resource matters."}, {"n": 10, "q": "A chain of generators does", "options": ["Only the work the consumer asks for, unless something in the middle is eager", "All the work up front", "The work in parallel", "The work twice"], "answer": 0, "why": "One `sorted`, one list comprehension or one `len` in the middle forces everything through it."}, {"n": 11, "q": "`sorted(gen)` inside a pipeline", "options": ["Preserves laziness", "Consumes the whole source before it can yield anything", "Raises on an infinite generator only", "Is the same as `list(gen)`"], "answer": 1, "why": "It cannot return its first value until it has seen the last input, which is what makes it eager."}, {"n": 12, "q": "A paused generator holds", "options": ["Nothing", "Its frame, including all its local variables", "A copy of the values produced so far", "The consumer's stack"], "answer": 1, "why": "Which is the memory cost: the saving is against materialising the values, not against having none."}, {"n": 13, "q": "`inspect.getgeneratorstate(g)` on a generator that has never run gives", "options": ["`GEN_CREATED`", "`GEN_SUSPENDED`", "`GEN_RUNNING`", "`GEN_CLOSED`"], "answer": 0, "why": "Which is exactly why `send` with a non-None value fails there."}, {"n": 14, "q": "Prefer a generator function over a generator expression when", "options": ["The input is large", "The logic has structure: a condition, state between items, a `try`, or a name worth having", "You need it to be lazy", "The output is a dict"], "answer": 1, "why": "Roughly the point where a comprehension stops being readable, which is not a coincidence."}, {"n": 15, "q": "Returning a generator when the caller needs the length is", "options": ["Fine; they can call `len`", "Unkind: they have to materialise it, so the function may as well", "Faster", "Required by the protocol"], "answer": 1, "why": "The same holds when they need it twice or need to index it."}]