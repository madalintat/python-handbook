[{"n": 1, "q": "`item in some_list` versus `item in some_set`", "options": ["Both scan", "The list scans; the set hashes and does not", "Both hash", "The set is slower for small collections"], "answer": 1, "why": "Which is why a membership test inside a loop over the same collection is the commonest reason Python code is unexpectedly slow."}, {"n": 2, "q": "Dict insertion order is", "options": ["An implementation detail of CPython", "Part of the language since 3.7", "Only preserved for string keys", "Alphabetical"], "answer": 1, "why": "Sets are a different matter: they have no order at all, despite looking like a dict without values."}, {"n": 3, "q": "Re-assigning an existing dict key", "options": ["Leaves it in its original position", "Moves it to the end", "Moves it to the front", "Raises"], "answer": 0, "why": "Only deleting and re-adding moves a key, because the position is fixed at first insertion."}, {"n": 4, "q": "When a missing key means the caller made a mistake, use", "options": ["`d[key]`", "`d.get(key)`", "`d.get(key, None)`", "`d.setdefault(key)`"], "answer": 0, "why": "`get` converts a missing key into a `None` that fails somewhere else. `d[key]` raises naming the key, at the point of the mistake."}, {"n": 5, "q": "`d.setdefault(key, expensive())` calls `expensive()`", "options": ["Only when the key is missing", "Every time, because arguments are evaluated before the call", "Never, it is lazy", "Once, at definition time"], "answer": 1, "why": "Which is what `defaultdict` fixes: it takes a factory and calls it only when a missing key is read."}, {"n": 6, "q": "Reading a missing key from a `defaultdict`", "options": ["Raises KeyError", "Returns the factory's value without storing it", "Inserts it, so the lookup is also a write", "Returns None"], "answer": 2, "why": "Which is why `dict(table)` once the building is done hands the rest of the program something that raises on a typo."}, {"n": 7, "q": "`.items()` returns", "options": ["A list of pairs", "A live view onto the dict", "An iterator that is consumed", "A copy"], "answer": 1, "why": "Views are cheap and reflect later changes, which is also why changing a dict's size mid-iteration raises."}, {"n": 8, "q": "`d.keys() & other.keys()` gives", "options": ["The keys present in both", "A TypeError", "The merged dicts", "The values in both"], "answer": 0, "why": "Key views support set operations, which is a genuinely useful and little-known thing about them."}, {"n": 9, "q": "`{**defaults, **overrides}`", "options": ["Builds a new dict where the later keys win", "Modifies `defaults`", "Builds a new dict where the earlier keys win", "Raises on a duplicate key"], "answer": 0, "why": "`defaults | overrides` is the same thing since 3.9. `update` and `|=` are the in-place versions."}, {"n": 10, "q": "The empty set is written", "options": ["`{}`", "`set()`", "`set[]`", "`frozenset`"], "answer": 1, "why": "`{}` is an empty dict. A set comprehension and a dict comprehension differ only by the colon."}, {"n": 11, "q": "`tags | [\"x\"]` where `tags` is a set", "options": ["Returns a set with \"x\" added", "Raises TypeError", "Returns a list", "Ignores the list"], "answer": 1, "why": "The operators require both sides to be sets; the method forms accept any iterable, so `tags.union([\"x\"])` works."}, {"n": 12, "q": "`list(set(items))` versus `list(dict.fromkeys(items))`", "options": ["They are the same", "Both deduplicate; only the second keeps first-seen order", "Only the first deduplicates", "The second is not valid"], "answer": 1, "why": "Dicts preserve insertion order and sets have none, which makes `fromkeys` the idiom for ordered deduplication."}, {"n": 13, "q": "Why must a dict key's hash not change while it is in the table?", "options": ["It would slow lookups down", "Nothing moves the entry, so lookups compute a different slot and never find it", "It would raise immediately", "It would break iteration order"], "answer": 1, "why": "The key is still visible when you iterate. It is only unreachable by lookup, which is what makes it confusing."}, {"n": 14, "q": "A collision between two keys", "options": ["Loses one of them", "Is routine: the table probes nearby slots and compares as it goes", "Raises", "Forces a resize"], "answer": 1, "why": "It is the other direction, equal keys hashing differently, that actually breaks a table."}, {"n": 15, "q": "An object's attributes are stored in", "options": ["A list", "A dict, visible as `obj.__dict__`", "A fixed array, always", "The class"], "answer": 1, "why": "Which is why `__slots__` saves memory by removing that per-instance dict, and why attribute access performs like a dict lookup."}]