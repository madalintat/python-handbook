[{"n": 1, "q": "`ruff`, `mypy` and `pytest` together", "options": ["Overlap almost not at all, which is why running all three is normal", "Are three ways to do the same check", "Conflict, so pick one", "Replace each other in that order"], "answer": 0, "why": "A linter cannot know a type is wrong, a checker cannot know a test fails, and neither can tell you a function is unused."}, {"n": 2, "q": "The `B` rule family, `bugbear`, is worth adding because", "options": ["Its rules are about behaviour rather than style", "It is the fastest", "It replaces mypy", "It is on by default"], "answer": 0, "why": "The mutable default, the loop variable captured in a closure, the bare `except`."}, {"n": 3, "q": "The `UP` family", "options": ["Rewrites code to newer syntax, mostly automatically with `--fix`", "Upgrades dependencies", "Checks the Python version", "Updates type stubs"], "answer": 0, "why": "How a codebase stops accumulating `typing.List` and `%`-formatting."}, {"n": 4, "q": "`# noqa` with no code", "options": ["Silences every rule on that line, including the one that appears there next year", "Silences the first rule", "Is a syntax error", "Is the recommended form"], "answer": 0, "why": "The same argument as `# type: ignore`, in a different tool. Every suppression worth using takes a code."}, {"n": 5, "q": "A rule that needs suppressing in twenty files", "options": ["Is a rule you have decided against; say so once in `pyproject.toml`", "Should be suppressed in twenty files", "Should be fixed in all twenty", "Means the linter is wrong"], "answer": 0, "why": "One config line is honest, greppable and reviewable. Twenty comments are the same decision made invisibly."}, {"n": 6, "q": "The argument for a formatter is", "options": ["It ends the discussion permanently, which is worth more than any style it picks", "Its style is the best one", "It finds bugs", "It is required by CI"], "answer": 0, "why": "It also shrinks diffs, because nobody reformats a file while editing it."}, {"n": 7, "q": "`zip(a, b)` with lists of different lengths", "options": ["Stops at the shorter and says nothing, which `B905` asks you to decide about", "Raises", "Pads with `None`", "Repeats the shorter"], "answer": 0, "why": "`strict=True` raises on a mismatch; `strict=False` says you meant to truncate."}, {"n": 8, "q": "A bare `except:` differs from `except Exception:` in that it catches", "options": ["`KeyboardInterrupt`, `SystemExit` and `GeneratorExit` as well", "Nothing extra", "Only syntax errors", "Warnings too"], "answer": 0, "why": "Those three sit outside `Exception` on purpose. A program that swallows Ctrl-C is the classic symptom."}, {"n": 9, "q": "`raise X from err` inside an `except`", "options": ["Records the original as the cause, so the traceback shows both", "Replaces the original", "Suppresses the original", "Re-raises the original"], "answer": 0, "why": "`from None` deliberately suppresses it. `B904` asks you to say which you meant."}, {"n": 10, "q": "Iterating `dict.keys()` while deleting from the dict", "options": ["Raises, because `.keys()` is a view of the dict as it changes", "Works", "Skips entries silently", "Is undefined"], "answer": 0, "why": "`list(cache)` takes a snapshot. Building the result and rebinding is usually better still."}, {"n": 11, "q": "Pre-commit hooks should be", "options": ["Fast, because a slow hook gets bypassed and a bypassed hook is worse than none", "Thorough", "The only place checks run", "Optional for the team"], "answer": 0, "why": "A bypassed hook produces the belief that the check is running."}, {"n": 12, "q": "The same checks must run in CI because", "options": ["Pre-commit can be skipped and CI cannot", "CI is faster", "Pre-commit does not support all tools", "It is required by the standard"], "answer": 0, "why": "Pre-commit catches things early; CI is what enforces them."}, {"n": 13, "q": "One flaky test", "options": ["Teaches everybody to re-run the job without looking, after which the suite is decorative", "Is acceptable if it is rare", "Should be retried automatically", "Only matters in CI"], "answer": 0, "why": "Fix or delete it the day you find it."}, {"n": 14, "q": "`hypothesis` is worth reaching for when", "options": ["There is a clear invariant: an exact round trip, a stable sort, a parser that must never crash", "The test suite is slow", "Coverage is low", "You have no test cases"], "answer": 0, "why": "It generates inputs you would not have thought of, then shrinks a failure to the smallest one that reproduces."}, {"n": 15, "q": "The habit that most reliably prevents the same bug twice is", "options": ["After fixing one, asking whether a check could have caught it, and adding that check", "Writing more tests", "Raising the coverage target", "A stricter linter"], "answer": 0, "why": "Not always a test: sometimes a lint rule, sometimes an annotation that makes the mistake unrepresentable."}]